function renderAdventCalendar() {
    // code;
}

function getStatusByDate(date){
    let now = new Date();
    if(Date.now()>date) return "pass";
    if(Date.now()<)
}

class Pot{
    constructor(height, width, date){
        this.height = height;
        this.width = width;
        this.date = date;
        status = getStatusByDate(date);
    }
}